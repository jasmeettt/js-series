"use strict"; //treat all JS code as newer version.

// alert("hello") // we are using nodejs, not browser.

console.log(3+
    3);//❌  
console.log("Jasmeet"); // code readability should always be high.

let name = "Jasmeet";
let age = 20;
let isloggedIn = true;

/*
Number => 2 to power of 53 (range for numbers)
string => ("", '')
boolean => (yes,no True,false)
null => stand alone value
undefined => (values not assigned but the variable is)
symbol => (used mostly in react) uniqueness 

object
*/

console.log(typeof"Jasmeet");
console.log(typeof null);
console.log(typeof undefined);

